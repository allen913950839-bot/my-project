# 🔄 GameSoul-Interactive 自动部署工作流

## 📋 完整流程概览

```
本地代码修改 → Git 提交 → 推送到 GitHub → Vercel 自动检测 → 自动构建部署 → 更新在线链接
```

---

## 🚀 首次设置（只需一次）

### 方法一：使用自动脚本（推荐）

在终端执行：

```bash
cd /Users/allenzqwei/Desktop/playtest/GameSoul-Interactive
./deploy-to-github.sh
```

脚本会引导你：
1. 配置 Git 用户信息
2. 输入 GitHub 用户名
3. 自动推送代码到 GitHub
4. 显示 Vercel 连接步骤

### 方法二：手动设置

#### 步骤 1：创建 GitHub 仓库

1. 访问：https://github.com/new
2. 仓库名：`gamesoul-interactive`
3. 描述：`GameSoul - AI游戏互动评价平台`
4. 选择 Public 或 Private
5. **不要**勾选任何初始化选项
6. 点击 "Create repository"

#### 步骤 2：推送代码到 GitHub

```bash
cd /Users/allenzqwei/Desktop/playtest/GameSoul-Interactive

# 关联仓库（替换 YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/gamesoul-interactive.git

# 推送代码
git branch -M main
git push -u origin main
```

**如果提示需要认证**：

使用 Personal Access Token 代替密码：

1. 创建 Token：https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 权限选择：`repo`（完整仓库访问）
4. 生成并复制 token
5. 推送时使用：
   ```bash
   git push https://YOUR_TOKEN@github.com/YOUR_USERNAME/gamesoul-interactive.git main
   ```

#### 步骤 3：连接 Vercel（实现自动部署）

1. **访问 Vercel**
   ```
   https://vercel.com
   ```

2. **使用 GitHub 登录**
   - 点击 "Continue with GitHub"
   - 授权 Vercel 访问你的 GitHub 账户

3. **导入项目**
   - 点击 "Add New..." → "Project"
   - 点击 "Import Git Repository"
   - 找到并选择 `gamesoul-interactive` 仓库

4. **配置项目**
   
   Vercel 会自动检测到：
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
   
   **✅ 保持默认配置即可**

5. **添加环境变量（重要！）**
   
   展开 "Environment Variables" 部分：
   
   ```
   Name:  VITE_GEMINI_API_KEY
   Value: AIzaSyBrDHxvH9MfAXLW-Jwu5huJfrjA6XwX6Sk
   
   Environment: ✅ Production ✅ Preview ✅ Development
   ```

6. **部署**
   - 点击 "Deploy"
   - 等待 1-2 分钟
   - 获得链接：`https://gamesoul-interactive.vercel.app`

---

## 🔄 日常开发工作流

### 第一次设置完成后，每次更新只需：

```bash
# 1. 进入项目目录
cd /Users/allenzqwei/Desktop/playtest/GameSoul-Interactive

# 2. 修改代码...（在编辑器中修改）

# 3. 查看修改
git status

# 4. 添加所有修改
git add .

# 5. 提交（附上有意义的说明）
git commit -m "描述你的修改内容"

# 6. 推送到 GitHub
git push
```

**就这么简单！** 推送后：
- ⚡ GitHub 接收代码
- ⚡ Vercel 自动检测到更新
- ⚡ 自动构建新版本
- ⚡ 自动部署到生产环境
- ⚡ 你的在线链接自动更新

**整个过程约 1-2 分钟，无需手动操作！**

---

## 📝 常用 Git 命令

### 查看项目状态
```bash
git status
```

### 查看修改内容
```bash
git diff
```

### 查看提交历史
```bash
git log --oneline
```

### 撤销修改（未提交）
```bash
# 撤销单个文件
git checkout -- filename

# 撤销所有修改
git checkout -- .
```

### 查看远程仓库
```bash
git remote -v
```

---

## 🎯 快速更新脚本

创建一个快速更新脚本 `update.sh`：

```bash
#!/bin/bash
# 快速更新并部署

echo "📝 请输入提交说明："
read commit_message

git add .
git commit -m "$commit_message"
git push

echo "✅ 代码已推送！"
echo "⏳ Vercel 正在自动部署..."
echo "🔗 访问：https://gamesoul-interactive.vercel.app"
```

使用方法：
```bash
chmod +x update.sh
./update.sh
```

---

## 🔍 Vercel 部署面板功能

访问：https://vercel.com/dashboard

### 1. 部署列表
- 查看所有部署历史
- 每次 git push 创建一个新部署
- 可以回滚到任何历史版本

### 2. 实时日志
- 查看构建日志
- 查看错误信息
- 调试部署问题

### 3. 预览部署
- 每个分支自动创建预览链接
- Pull Request 自动部署预览
- 测试后再合并到主分支

### 4. 环境变量管理
- 添加/修改环境变量
- 分别配置 Production/Preview/Development
- 修改后需要重新部署

### 5. 域名设置
- 添加自定义域名
- 自动 HTTPS 证书
- DNS 配置指南

---

## 🌿 多环境部署

### 开发环境（本地）
```bash
npm run dev
# http://localhost:5173
```

### 预览环境（Vercel）
创建新分支推送：
```bash
git checkout -b feature/new-feature
git add .
git commit -m "新功能开发"
git push origin feature/new-feature
```

Vercel 会自动创建预览链接：
```
https://gamesoul-interactive-xxx.vercel.app
```

### 生产环境（Vercel）
合并到 main 分支：
```bash
git checkout main
git merge feature/new-feature
git push
```

自动部署到：
```
https://gamesoul-interactive.vercel.app
```

---

## 📊 自动部署优势

### ✅ 节省时间
- 无需手动构建
- 无需手动上传
- 无需配置服务器

### ✅ 减少错误
- 自动化流程
- 版本控制
- 可追溯历史

### ✅ 协作友好
- 团队成员直接推送
- 自动部署
- 预览链接分享

### ✅ 快速迭代
- 推送即部署
- 1-2 分钟上线
- 即时查看效果

---

## 🔔 部署通知

Vercel 可以发送部署通知：

1. 访问项目设置：Settings → Notifications
2. 添加通知渠道：
   - Email（邮件通知）
   - Slack（团队协作）
   - Discord（社区）
   - Webhook（自定义）

---

## 🐛 常见问题

### Q1: git push 提示认证失败
**A**: 使用 Personal Access Token 代替密码：
```bash
git remote set-url origin https://YOUR_TOKEN@github.com/USERNAME/gamesoul-interactive.git
git push
```

### Q2: Vercel 部署失败
**A**: 检查：
1. 构建日志中的错误信息
2. package.json 依赖是否完整
3. 环境变量是否配置

### Q3: 推送后 Vercel 没有自动部署
**A**: 检查：
1. Vercel 项目是否连接到正确的 GitHub 仓库
2. 推送的分支是否是 main
3. Vercel 项目设置中 Git 集成是否启用

### Q4: 部署后环境变量不生效
**A**: 
1. 在 Vercel 项目设置中添加环境变量
2. 变量名必须以 `VITE_` 开头
3. 修改后需要重新部署（触发一次 git push）

### Q5: 想要回滚到之前的版本
**A**: 
1. 在 Vercel 部署列表中找到目标版本
2. 点击 "Promote to Production"
3. 或使用 Git 回滚：
   ```bash
   git revert HEAD
   git push
   ```

---

## 📈 性能监控

Vercel 提供免费的性能监控：

1. **Analytics**
   - 访问量统计
   - 地理位置分布
   - 设备类型分析

2. **Speed Insights**
   - 页面加载速度
   - Core Web Vitals
   - 性能建议

3. **Logs**
   - 实时请求日志
   - 错误追踪
   - 调试信息

访问：https://vercel.com/dashboard → 选择项目 → Analytics

---

## 🎉 成功案例

完成自动部署后，你的工作流：

```
1. 修改代码（在 VS Code/IDE 中）
   ↓
2. git add . && git commit -m "..." && git push
   ↓
3. 喝杯咖啡 ☕️（等待 1-2 分钟）
   ↓
4. 访问链接，查看更新效果 ✨
```

**对比传统部署**：
- ❌ 手动打包：`npm run build`
- ❌ 上传到服务器：FTP/SSH
- ❌ 配置 Nginx/Apache
- ❌ 配置 HTTPS
- ❌ 处理域名解析
- ❌ 服务器维护

**使用 GitHub + Vercel**：
- ✅ `git push`
- ✅ 完成！

---

## 📞 获取帮助

- Vercel 文档：https://vercel.com/docs
- GitHub 文档：https://docs.github.com
- Git 教程：https://git-scm.com/book/zh/v2

---

**祝自动部署顺利！** 🚀

*更新时间：2025-11-20*
