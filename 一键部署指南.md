# 🎯 GameSoul-Interactive 一键部署指南

## ✅ 已完成的准备工作

1. ✅ 修复了 Gemini API（从 v1beta 升级到 v1，使用 gemini-1.5-flash）
2. ✅ 修复了 vite.svg 404 错误
3. ✅ 代码已提交到本地 Git
4. ✅ 创建了自动部署脚本

---

## 🚀 三步完成部署

### 步骤 1：创建 GitHub 仓库（2分钟）

1. **打开浏览器，访问**：
   ```
   https://github.com/new
   ```

2. **填写仓库信息**：
   - Repository name: `gamesoul-interactive`
   - Description: `GameSoul - AI游戏互动评价平台`  
   - 选择 **Public**（公开）或 **Private**（私有）
   - ⚠️ **不要勾选**任何初始化选项（README、.gitignore、License）

3. **点击 "Create repository"**

4. **记下你的 GitHub 用户名**（稍后需要用）

---

### 步骤 2：推送代码到 GitHub（1分钟）

**在终端执行以下命令**（替换 `YOUR_USERNAME` 为你的 GitHub 用户名）：

```bash
cd /Users/allenzqwei/Desktop/playtest/GameSoul-Interactive

# 关联 GitHub 仓库
git remote add origin https://github.com/YOUR_USERNAME/gamesoul-interactive.git

# 推送代码
git push -u origin main
```

**如果提示需要认证**：

GitHub 不再支持密码登录，需要使用 Personal Access Token：

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选权限：`repo`（完整仓库访问）
4. 点击 "Generate token"
5. **复制生成的 token**（只显示一次！）

然后执行：
```bash
git remote set-url origin https://YOUR_TOKEN@github.com/YOUR_USERNAME/gamesoul-interactive.git
git push -u origin main
```

---

### 步骤 3：连接 Vercel 实现自动部署（3分钟）

1. **访问 Vercel**：
   ```
   https://vercel.com
   ```

2. **使用 GitHub 登录**
   - 点击 "Continue with GitHub"
   - 授权 Vercel 访问

3. **导入项目**
   - 点击 "Add New..." → "Project"
   - 点击 "Import Git Repository"
   - 找到 `gamesoul-interactive` 仓库并点击 "Import"

4. **配置项目**（保持默认即可）
   - Framework Preset: **Vite** ✅
   - Root Directory: **./** ✅
   - Build Command: **`npm run build`** ✅
   - Output Directory: **`dist`** ✅

5. **配置环境变量（重要！）**
   
   展开 "Environment Variables"，添加：
   
   | Name | Value |
   |------|-------|
   | `VITE_GEMINI_API_KEY` | `AIzaSyBrDHxvH9MfAXLW-Jwu5huJfrjA6XwX6Sk` |
   
   环境选择：✅ Production ✅ Preview ✅ Development

6. **点击 "Deploy"**

7. **等待 1-2 分钟**，部署完成！

8. **获取访问链接**：
   ```
   https://gamesoul-interactive.vercel.app
   ```

---

## 🎉 完成！自动部署已启用

现在，每次你修改代码后：

```bash
# 1. 修改代码...

# 2. 提交并推送
git add .
git commit -m "你的修改说明"
git push

# 3. Vercel 自动部署！无需任何手动操作！
```

**或者使用快捷脚本**：

```bash
./update.sh
```

---

## 📱 分享链接给客户

部署成功后，发送给客户：

```
🎮 GameSoul-Interactive 体验链接

👉 https://gamesoul-interactive.vercel.app

✨ 功能亮点：
• AI 智能对话（Gemini 驱动）
• 王者荣耀亚瑟毒舌互动
• 和平精英角色对话
• 输入"抽"3次触发彩蛋
• Shift+Enter 生成评价卡片

支持所有设备访问 📱💻
```

---

## 🔄 自动部署工作流程

```
你修改代码
    ↓
git push 推送
    ↓
GitHub 接收代码
    ↓
Vercel 自动检测更新
    ↓
自动构建项目
    ↓
自动部署到生产环境
    ↓
在线链接自动更新 ✨
```

**整个过程：1-2 分钟，全自动！**

---

## 🛠️ 日常更新命令

### 方式一：使用快捷脚本（推荐）

```bash
./update.sh
```

### 方式二：手动命令

```bash
git add .
git commit -m "描述你的修改"
git push
```

---

## 📊 Vercel 功能

访问：https://vercel.com/dashboard

- **Deployments**：查看所有部署历史
- **Analytics**：访问量统计
- **Logs**：实时日志和错误追踪
- **Settings**：
  - Environment Variables（环境变量管理）
  - Domains（自定义域名）
  - Git（仓库连接管理）

---

## ⚡ 性能优化建议

### 1. 自定义域名（可选）

在 Vercel 项目设置中：
- Settings → Domains
- 添加自定义域名（免费）
- 自动配置 HTTPS

### 2. 预览部署

创建功能分支：
```bash
git checkout -b feature/new-feature
# 修改代码...
git add .
git commit -m "新功能"
git push origin feature/new-feature
```

Vercel 会自动创建预览链接，测试通过后再合并到 main。

---

## 🐛 常见问题解决

### Q: git push 要求输入密码但失败
**A**: 使用 Personal Access Token，参考步骤 2

### Q: Vercel 找不到我的仓库
**A**: 在 Vercel 点击 "Adjust GitHub App Permissions"，授权访问仓库

### Q: 部署后 AI 仍然不工作
**A**: 检查：
1. Vercel 环境变量是否添加 `VITE_GEMINI_API_KEY`
2. 变量值是否正确
3. 重新部署项目（触发一次 git push）

### Q: 修改代码后推送，但网站没更新
**A**: 
1. 检查 Vercel 部署列表，确认部署成功
2. 清除浏览器缓存（Ctrl+Shift+R 或 Cmd+Shift+R）
3. 检查推送的分支是否是 main

---

## 📞 需要帮助？

- Vercel 文档：https://vercel.com/docs
- GitHub 文档：https://docs.github.com
- Gemini API 文档：https://ai.google.dev/docs

---

## 🎯 下一步

完成部署后，你可以：

1. ✅ 分享链接给客户体验
2. ✅ 继续开发新功能（自动部署）
3. ✅ 监控访问数据（Vercel Analytics）
4. ✅ 优化性能和用户体验
5. ✅ 添加自定义域名

---

**祝部署成功！** 🚀

如有问题，随时询问！

*更新时间：2025-11-20*
