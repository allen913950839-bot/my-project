# 🏗️ GameSoul 架构说明

## 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      GameSoul 应用                           │
│                     (React SPA)                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      App.jsx (根组件)                         │
│  ┌────────────────────────────────────────────────────┐    │
│  │ 状态管理                                             │    │
│  │ - currentView: 'universe' | 'soullink' | 'memory'  │    │
│  │ - selectedGame: Game Object                        │    │
│  │ - reviewData: Review Object                        │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                ▼             ▼             ▼
        ┌───────────┐  ┌──────────┐  ┌──────────┐
        │  Universe │  │ SoulLink │  │  Memory  │
        │  View     │  │  View    │  │  Card    │
        └───────────┘  └──────────┘  └──────────┘
```

## 组件层级结构

```
App.jsx (根组件)
│
├── GameUniverse (游戏宇宙选择器)
│   │
│   ├── 标题区域
│   │   ├── Sparkles 图标
│   │   ├── "GameSoul" 标题
│   │   └── 副标题
│   │
│   ├── 发行商列表
│   │   └── 发行商卡片 (Tencent)
│   │       ├── 发行商图标 (Gamepad2)
│   │       ├── 发行商名称
│   │       └── 展开指示器 (ChevronRight)
│   │
│   └── 游戏列表（展开时）
│       └── 游戏卡片 (王者荣耀)
│           ├── 角色头像 (🦊)
│           ├── 游戏信息
│           └── 进入按钮
│
├── SoulLink (灵魂链接界面)
│   │
│   ├── 顶部导航栏
│   │   ├── 返回按钮 (ArrowLeft)
│   │   ├── 游戏标题
│   │   └── 角色名称
│   │
│   ├── 角色展示区
│   │   ├── 背景光晕效果
│   │   ├── 角色头像（动画）
│   │   ├── 情绪指示器
│   │   │   ├── Heart (开心)
│   │   │   ├── HeartCrack (难过)
│   │   │   └── Sparkles (中性)
│   │   └── 角色名称
│   │
│   ├── 聊天区域
│   │   ├── 消息列表（可滚动）
│   │   │   ├── 角色消息气泡
│   │   │   └── 用户消息气泡
│   │   └── "思考中" 动画
│   │
│   └── 输入区域
│       ├── 文本输入框
│       ├── 发送按钮 (Send)
│       ├── 语音按钮 (Mic)
│       └── 生成记忆卡按钮 (Sparkles)
│
└── MemoryCard (记忆卡片)
    │
    ├── 顶部导航
    │   ├── 返回按钮
    │   ├── 下载按钮 (Download)
    │   └── 分享按钮 (Share2)
    │
    ├── 卡片主体
    │   ├── 背景光晕
    │   ├── 角色头像（大）
    │   ├── 游戏名称
    │   ├── 角色标识
    │   ├── 分割线
    │   ├── 用户引用
    │   ├── 星级评分 (Star × 5)
    │   ├── 情感标签
    │   ├── 时间戳
    │   └── 装饰边框
    │
    ├── 描述文字
    └── 操作按钮
```

## 数据流图

```
用户交互
    │
    ▼
┌─────────────────┐
│  用户点击游戏    │
└─────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ GameUniverse.onGameSelect()     │
│ ├─ 传递游戏对象                 │
│ └─ 触发 App.handleGameSelect()  │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ App 状态更新                     │
│ ├─ setSelectedGame(game)        │
│ └─ setCurrentView('soullink')   │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────┐
│ SoulLink 渲染   │
└─────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ 用户输入消息                     │
│ ├─ 输入文本                     │
│ └─ 点击发送                     │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ SoulLink.handleSendMessage()    │
│ ├─ 添加用户消息到列表           │
│ ├─ 调用 analyzeSentiment()      │
│ ├─ 调用 getDajiResponse()       │
│ ├─ 更新角色情绪状态             │
│ └─ 添加 AI 回复到列表           │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ 用户点击"生成记忆卡"             │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ SoulLink.handleGenerateMemory() │
│ ├─ 提取所有用户消息             │
│ ├─ 分析整体情感                 │
│ ├─ 计算星级评分                 │
│ ├─ 生成引用文本                 │
│ └─ 调用 onReviewComplete()      │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ App 状态更新                     │
│ ├─ setReviewData(data)          │
│ └─ setCurrentView('memory')     │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────┐
│ MemoryCard 渲染 │
└─────────────────┘
```

## 核心算法流程

### 情感分析算法

```
用户输入文本
    │
    ▼
┌─────────────────────────────┐
│ analyzeSentiment(text)      │
│                             │
│ 1. 定义关键词库             │
│    ├─ negative: ['差','烂'] │
│    └─ positive: ['好','棒'] │
│                             │
│ 2. 转换为小写               │
│                             │
│ 3. 统计关键词匹配           │
│    ├─ negativeCount         │
│    └─ positiveCount         │
│                             │
│ 4. 判断情感类型             │
│    ├─ neg > pos → negative  │
│    ├─ pos > neg → positive  │
│    └─ 其他 → neutral        │
└─────────────────────────────┘
    │
    ▼
返回: 'negative' | 'positive' | 'neutral'
```

### AI 回复生成流程

```
(用户消息, 情感类型)
    │
    ▼
┌─────────────────────────────┐
│ getDajiResponse(msg, sent)  │
│                             │
│ 1. 准备回复模板库           │
│    ├─ negative: [回复1...] │
│    ├─ positive: [回复2...] │
│    └─ neutral: [回复3...]  │
│                             │
│ 2. 根据情感选择模板组       │
│                             │
│ 3. 随机选择一条回复         │
│    Math.random() × length   │
│                             │
│ 4. 返回回复文本             │
└─────────────────────────────┘
    │
    ▼
显示 AI 回复（带表情符号）
```

### 记忆卡生成流程

```
对话历史
    │
    ▼
┌─────────────────────────────┐
│ handleGenerateMemory()      │
│                             │
│ 1. 过滤用户消息             │
│    messages.filter(user)    │
│                             │
│ 2. 合并所有文本             │
│    join(' ')                │
│                             │
│ 3. 分析整体情感             │
│    analyzeSentiment()       │
│                             │
│ 4. 计算星级                 │
│    ├─ positive → 5 星       │
│    ├─ negative → 2 星       │
│    └─ neutral → 3 星        │
│                             │
│ 5. 提取引用                 │
│    (最后一条用户消息)        │
│                             │
│ 6. 生成数据对象             │
│    { messages, sentiment,   │
│      stars, quote }         │
└─────────────────────────────┘
    │
    ▼
传递给 MemoryCard 组件渲染
```

## 动画系统架构

### Framer Motion 使用模式

```
┌─────────────────────────────────┐
│ 页面级动画                       │
│                                 │
│ AnimatePresence (mode="wait")   │
│   ├─ GameUniverse              │
│   │   ├─ initial: opacity 0    │
│   │   ├─ animate: opacity 1    │
│   │   └─ exit: opacity 0       │
│   │                            │
│   ├─ SoulLink                  │
│   │   └─ (同上)                │
│   │                            │
│   └─ MemoryCard                │
│       └─ (同上)                │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ 组件级动画                       │
│                                 │
│ GameUniverse:                   │
│   ├─ 标题: y: -50 → 0          │
│   ├─ 卡片: x: -100 → 0         │
│   └─ 游戏: height: 0 → auto    │
│                                 │
│ SoulLink:                       │
│   ├─ 导航栏: y: -100 → 0       │
│   ├─ 角色: scale: 0.8 → 1      │
│   ├─ 浮动: translateY 循环     │
│   └─ 消息: y: 20 → 0           │
│                                 │
│ MemoryCard:                     │
│   ├─ 卡片: rotateY: -90 → 0    │
│   ├─ 光晕: scale 呼吸          │
│   └─ 星星: 逐个弹出            │
└─────────────────────────────────┘
```

## 状态管理架构

```
App.jsx (状态提升)
│
├── currentView: string
│   ├─ 'universe'  → 显示游戏选择
│   ├─ 'soullink'  → 显示聊天界面
│   └─ 'memory'    → 显示记忆卡
│
├── selectedGame: object | null
│   ├─ id: 游戏ID
│   ├─ name: 游戏名称
│   ├─ character: 角色信息
│   └─ theme: 主题颜色
│
└── reviewData: object | null
    ├─ messages: 消息列表
    ├─ sentiment: 整体情感
    ├─ stars: 星级评分
    └─ quote: 引用文本

Props 传递：
│
├── GameUniverse
│   └─ onGameSelect(game)
│       → App.handleGameSelect
│           → setSelectedGame(game)
│           → setCurrentView('soullink')
│
├── SoulLink
│   ├─ game (prop)
│   ├─ onReviewComplete(data)
│   │   → App.handleReviewComplete
│   │       → setReviewData(data)
│   │       → setCurrentView('memory')
│   └─ onBack()
│       → App.handleBackToUniverse
│
└── MemoryCard
    ├─ data (prop)
    ├─ game (prop)
    └─ onBack()
        → App.handleBackToUniverse
```

## 样式系统架构

```
┌─────────────────────────────────┐
│ Tailwind CSS 配置               │
│                                 │
│ theme.extend:                   │
│   ├─ colors                     │
│   │   ├─ cyber-purple           │
│   │   ├─ cyber-pink             │
│   │   ├─ cyber-blue             │
│   │   ├─ cyber-gold             │
│   │   ├─ dark-bg                │
│   │   └─ dark-card              │
│   │                             │
│   ├─ fontFamily                 │
│   │   └─ gaming: 'Orbitron'     │
│   │                             │
│   ├─ animation                  │
│   │   ├─ pulse-glow             │
│   │   └─ float                  │
│   │                             │
│   └─ keyframes                  │
│       ├─ pulse-glow (2s)        │
│       └─ float (3s)             │
└─────────────────────────────────┘

应用层级：
│
├─ 全局背景
│   ├─ bg-dark-bg
│   └─ 渐变层 (purple/blue)
│
├─ 卡片容器
│   ├─ bg-dark-card
│   └─ border-white/10
│
├─ 渐变边框
│   ├─ bg-gradient-to-r
│   └─ 主题色组合
│
└─ 文字样式
    ├─ font-gaming (标题)
    └─ text-white/gray (正文)
```

## 技术栈集成

```
┌──────────────────────────────────────────┐
│             浏览器环境                    │
└──────────────────────────────────────────┘
                    ▲
                    │
┌──────────────────────────────────────────┐
│             Vite 开发服务器               │
│  ├─ HMR (热模块替换)                     │
│  ├─ ESM (ES 模块)                        │
│  └─ 插件系统                             │
└──────────────────────────────────────────┘
                    ▲
                    │
┌──────────────────────────────────────────┐
│          React 18 + JSX                  │
│  ├─ 虚拟 DOM                             │
│  ├─ Hooks (useState, useRef, useEffect)  │
│  └─ 组件系统                             │
└──────────────────────────────────────────┘
                    ▲
                    │
        ┌───────────┴───────────┐
        ▼                       ▼
┌─────────────────┐    ┌─────────────────┐
│ Framer Motion   │    │  Tailwind CSS   │
│  ├─ motion.*    │    │  ├─ 实用类      │
│  ├─ AnimateP... │    │  ├─ JIT 编译    │
│  └─ Gestures    │    │  └─ 响应式      │
└─────────────────┘    └─────────────────┘
        │                       │
        └───────────┬───────────┘
                    ▼
            ┌───────────────┐
            │ Lucide React  │
            │ (图标库)      │
            └───────────────┘
```

## 性能优化策略

### 1. 代码分割
```
目前: 单一 bundle
未来可优化:
  ├─ React.lazy() 懒加载路由
  └─ 动态 import() 组件
```

### 2. 动画优化
```
使用:
  ├─ transform (GPU 加速)
  ├─ opacity (GPU 加速)
  └─ will-change (提前优化)

避免:
  ├─ width/height 动画
  └─ top/left 定位动画
```

### 3. 渲染优化
```
当前:
  ├─ 状态提升至 App
  └─ Props 向下传递

可优化:
  ├─ React.memo() 记忆组件
  ├─ useMemo() 记忆计算
  └─ useCallback() 记忆函数
```

---

**架构版本**: 1.0
**设计模式**: 状态提升 + Props 传递
**核心思想**: 组件化 + 动画驱动 + 情感交互
